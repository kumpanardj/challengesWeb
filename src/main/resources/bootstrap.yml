eureka:
  instance:
    non-secure-port: ${server.port:3333} # so that different instances of microservice have their own swagger Ui 
    statusPageUrlPath: /swagger-ui.html
    
  client:
   serviceUrl:
     defaultZone: hhttp://172.30.105.128:8761/eureka/
  